// root build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.1' // 사용 중인 Gradle 버전에 따라 다를 수 있음
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
